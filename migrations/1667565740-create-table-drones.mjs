export async function up(sql) {
  await sql`
    CREATE TABLE drones (
      id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
      registration_data varchar(110) NOT NULL,
      origin varchar(110) NOT NULL
    )
  `;
}

export async function down(sql) {
  await sql`
    DROP TABLE drones
    `;
}
